# 更新日志

本项目遵循语义化版本。小米运动健康云数据来自手机端已经上传的历史记录；所有版本均不提供蓝牙实时监护，也不构成医疗诊断。

## [v0.8.3] - 2026-08-06

### 新增与改进

- 生活数据调用判断模型现在会结合受限的最近文字对话与当前消息进行语义路由；用户无需直接询问指标，含蓄承接前文时也能判断相关生活数据是否有助于本轮回复。
- 配置页新增判断模型读取的最近消息条数以及是否包含 Bot 回复；系统消息、工具结果、图片和附件不会发送给判断模型。
- 判断需要数据后，插件只在成功取得相关记录时向当前聊天模型注入临时最小摘要；没有可用数据时完全跳过，不输出同步或缺失说明，也不干扰正常对话。
- 睡眠同步会优先探测并读取今天已经结束的睡眠记录，早晨同步后不再必须等到第二天才能看到本次睡眠；仍以小米云实际返回时间为准。

### 同步与稳定性

- 生活数据调用判断模型增加 3 秒硬超时；Provider 未及时结束时立即回退本地规则，不再长期占用对话前置流程。
- 普通对话等待云端刷新最多 2 秒，明确健康查询最多 5 秒；超时后当前回复使用本地缓存，已开始的同步继续在后台完成。
- 小米云正在连接、同步或诊断时，普通对话直接使用缓存或跳过生活数据，不再排队等待云端操作。
- `/健康连接` 改为静默启动后台检查；若已有云端操作则在后台等待，只在最终成功、失败或超时时发送一次结果，不阻塞普通聊天。
- 各数据类别增加 30 秒独立读取上限，单类超时不会中断其他类别；登录与区域识别不再占用数据下载预算。
- 后续同步按数据类别使用增量游标，并保留两天重叠范围，以兼顾延迟上传、云端修正与请求数量。

### 架构与维护

- 根目录 `main.py` 保留为 AstrBot 插件入口、生命周期和命令注册层；主动关心编排与自然对话数据路由迁移到独立 `features` 模块，降低后续修改互相影响的风险。
- 新增架构边界回归测试，防止对话路由逻辑重新堆回入口文件；现有配置键、命令、数据库和用户数据保持兼容。

## [v0.8.2] - 2026-07-29

### 安全与隐私

- 停用会被 AstrBot 记录到前台日志和普通对话历史的健康数据 LLM Tool；日常对话只使用带 `mark_as_temp()` 标记的临时摘要，不支持临时内容的旧运行时会安全跳过。
- 健康摘要授权与最近私聊授权拆分为两个默认关闭的独立开关；配置异常时隐私授权统一失败关闭。
- 所有健康数据入口和主动发送目标会精确校验使用者 UID、Bot ID、私聊类型与完整会话标识。
- 小米登录只跟随受信任小米域名下无用户信息、HTTPS 443 端口的重定向；登录及 API 响应限制读取大小，验证成功后才原子更新内存凭证。
- 自定义数据库增加绝对路径、允许后缀、符号链接、SQLite 文件头、插件归属和完整旧版结构指纹校验，并在每次连接时复核，降低误覆盖及运行期替换风险。
- 健康快照、同步时间与可选回复草稿在注入提示词前进行 HTML 转义；主动消息进一步拒绝 URI、Markdown 链接、裸域名和 Unicode 双向控制字符。
- `.gitignore` 覆盖三种允许的 SQLite 后缀及 WAL、SHM、journal 文件，减少健康缓存被误提交或打包的风险。

### 可靠性与维护

- 布尔和整数配置改为严格解析、范围限制和安全默认值，损坏或手工填写的配置不再导致插件启动崩溃或意外开启敏感功能。
- 云端分页、候选 key、连接探测和诊断共享请求条数及完整解压响应预算；心率、血氧和活动按流式顺序处理，降低异常响应造成的内存与请求放大。
- 小米云 `429` 限流使用共享冷却并立即停止当前批次；区域自动识别结果按账号摘要缓存，显式区域配置不会被自动覆盖。
- 对话按需刷新增加单飞、失败退避、60 秒硬冷却和最多 5 秒等待；手动同步失败不计入 60 秒软节流。
- 清除本地数据会停止全部后台任务并等待已开始的 SQLite 事务结束；任务取消、插件终止和主动关心持久化失败均增加竞态及重复发送保护。
- 明确运行环境需要 Python 3.11 及以上；测试可从任意解压目录名运行，并新增 Python 3.11/3.12 的 Ruff/pytest 配置和 GitHub Actions 发布前检查。

> 从 v0.8.1 或更早版本升级不会自动清理 AstrBot 已有的对话历史或运行日志；若曾开启健康数据模型授权并需要严格清理，请按部署环境自行删除旧历史与日志。

## [v0.8.1] - 2026-07-28

### 修复与改进

- 修复对话判断需要生活数据但使用本地缓存时前端日志完全静默的问题；现在会说明最近一次云端同步仍在刷新间隔内，但不会暗示所有类别都已有今天的新记录。
- 近期云端拉取失败进入退避时会说明当前回复暂用本地缓存；实际访问小米云仍只显示一次开始和完成状态。
- 对话运行报告仅包含数据类别，不记录聊天原文、账号、Token 或具体身体数值；无关对话和主动关心后台检查继续保持静默。
- 早安、刚醒和起床等日常语境统一按今天的起床日期读取睡眠；时间范围与类别连写、判断模型误选范围，或日常消息明确写出今天/昨天时，也不会再查询错误日期。
- 今天的睡眠汇总尚未返回时，自然对话会静默跳过睡眠类别并继续原话题；不会向用户解释同步、缓存或数据缺失。明确的排查命令仍可显示最近历史记录和同步状态。
- LLM Tool 在功能未开启、未授权、越权调用或没有可用数据时统一静默退出；缺少同步时间元数据时也不再向自然对话注入“暂无”等技术状态。

## [v0.8.0] - 2026-07-27

### 新增

- 配置页新增“生活数据调用判断提示词”和“深夜主动关心判断提示词”，均使用 AstrBot 官方 `text` 大文本框并提供可直接使用的默认内容。
- 深夜主动关心在规则筛出候选时机后，会由所选关心模型结合受限的最近文字私聊做严格布尔判断；只有明确返回 `send_care=true` 才生成并发送消息。
- 主动判断新增上下文来源、消息条数、上下文注入提示词和是否包含 Bot 消息四项设置；支持当前 LLM 对话历史、平台真实私聊流水与混合模式。

### 修复与改进

- 用户刚表示要睡觉、已经道过晚安、对话自然结束、机器人刚表达过相同关心或模型拿不准时，默认提示词要求保持安静。
- 最近一条所有者消息会在内存中即时保留，避免 AstrBot 尚未写入会话历史时错过“我要睡觉”等刚刚表达的意图；不会复制进插件 SQLite。
- 主动判断模型失败、输出异常或没有可用上下文时改为默认不发送，避免退化为机械提醒。
- 两类模型判断继续强制使用严格 JSON、长度上限和不可信文本隔离；自定义任务提示词不能取消这些安全边界。
- 日常对话实际触发按需云端拉取时，AstrBot 前端日志会各显示一次开始与完成状态；缓存命中和后台主动关心检查不输出，日志不包含凭证、聊天原文或具体生活数据。
- 平台流水只允许从已验证所有者私聊读取，混合模式会去重；平台流水不可用时回退到当前 LLM 对话历史。上下文最多 50 条、4000 字符，且始终排除图片、工具结果和系统消息。
- 平台流水改为核对插件数据库中由所有者事件绑定的完整私聊 UMO，不再假设 UMO 第三段一定等于 UID，兼容复合会话标识并继续拒绝其他会话。
- 修复 1～6 天本地保留期被静默扩大为至少 7 天的问题；现在按配置值执行，0 仍表示关闭自动清理。
- 未来的同步成功或失败时间不再错误阻止对话按需刷新；旧数据库中的无时区睡眠时间统一按 UTC 解释，避免显示日期偏移。
- 每个单元测试模块现在都能独立运行，不再依赖其他测试先加载 AstrBot 测试桩。

## [v0.7.0] - 2026-07-26

### 新增

- 配置页新增“生活数据调用判断模型”，可直接从 AstrBot 已配置的聊天模型中选择。
- 判断模型只接收当前所有者私聊文本，并以严格 JSON 决定是否需要生活数据、所需类别和时间范围；它不接收小米健康数据，也不负责生成最终回复。
- 判断需要数据后，插件仍按缓存新鲜度决定是否访问小米云，只向当前会话模型提供最多两个相关类别；当前回复最多等待刷新 5 秒，超时后使用已有缓存且后台同步继续。

### 修复与改进

- 改为智能按需同步优先：新安装默认关闭普通自动同步，已有安装保存的开关值不变；用户明确开启时仍按原配置间隔运行。
- 主动关心检查只读取本地缓存和近期私聊活动，不再无条件访问小米云；主动关心与普通自动同步成为可独立运行、不会重复创建的任务。
- 保留原始消息中的“刚同步”“最新”“刷新”等强制刷新意图，不再因判断模型生成分类焦点而丢失。
- 判断模型失败后按 1 分钟、5 分钟、15 分钟退避，期间立即使用本地规则；收到有效模型结果后清零。
- 云端记录时间增加合理范围和平台转换异常保护，单条负数、溢出或异常未来时间不会中断整个数据集。
- 隐私说明明确配置页密码遮罩不等于加密，并补充环境变量、目录权限和磁盘加密建议。

## [v0.6.1] - 2026-07-26

### 改进

- README 改为面向 AstrBot 普通用户的安装、配置和日常聊天指南，明确插件的主要用途是静默补充生活数据背景，而不是要求用户主动查询心率、睡眠或步数。
- 配置页名称与提示改为更直观的中文，突出普通聊天中的后台数据准备与自然人格回复；内部配置键保持不变，已有配置无需重新填写。
- 数据库、同步、隐私实现和维护测试细节迁移到独立的开发文档。

## [v0.6.0] - 2026-07-26

### 修复

- 独立卡路里记录不再生成零步活动日，避免步数 key 暂时返回空记录时覆盖本地正确步数。
- “最近 48 小时心率”改为读取完整时间范围，不再只用最新 100 条计算平均值和范围。
- 同步安全时限仅取消可安全中止的云端读取；已经开始的 SQLite 事务会在操作锁释放前完成，不再出现提示已取消但后台仍继续写入的情况。
- 本地保留期清理按 AstrBot 所有者隔离主动关心记录，不再影响共享数据库中的其他所有者。
- 拒绝把未来时间的私聊活动当作近期活跃；今日、昨日身体测量、血氧和压力使用对应日期文案。
- 限制并隔离模型工具的用户关注文本，降低超长输入和提示注入影响。
- 正式同步与诊断统一支持心率 `heart_rate` / `heartrate` / `hr` 和血氧 `spo2` / `blood_oxygen` 候选 key。
- 区域自动识别不再只依赖步数；没有近期记录时要求手动选择区域，不再静默落到错误区域。
- 活动按 `user_timezone` 分桶，并对同一分钟的手机/手环候选记录取最大值，降低重复计数；升级后的同步会清理同一记录遗留的旧时区日期。
- 自动同步仅在明确鉴权失效时暂停；普通网络故障按有上限的指数退避继续重试。
- 连接、同步和诊断共用同一操作锁，避免重连关闭正在使用的 HTTP 客户端。
- `/健康连接` 现在会关闭旧会话并重新验证凭证，不再把内存中的旧连接直接当作验证成功。
- 自然查询按所需数据类型判断新鲜度，记录单项失败，避免其他类别成功掩盖睡眠或心率失败。
- 自然查询会对最近失败的数据类别执行短期退避，避免每条日常消息都立即重试；明确要求“最新/刷新/同步”时仍可强制刷新。
- 普通对话改为使用本地缓存并触发单飞后台刷新，不再在主模型请求前等待最长一分钟云同步或额外模型草稿。
- 修复一个自然刷新批次失败时可能遗漏随后排队的其他数据类别；空或未知类别会在联网前明确拒绝。
- 修复主动监控与普通自动同步可能被同时重复启动的问题；状态页改为显示任务的实际运行状态。
- 心率和血氧会验证并合并所有候选 key；无效的首个别名不再遮住后续有效数据，候选请求出错且没有任何有效样本时也不会误记为同步成功。
- 昨日、今日和最近查询对活动、睡眠、身体测量、血氧及压力使用明确的本地日期范围，不再混入其他日期的记录。
- 为单 key、单数据集和云端读取增加记录数量及 5 分钟安全时限，限流响应同时支持秒数与 HTTP 日期格式的 `Retry-After`。
- 扩展凭证脱敏范围；远端业务错误只保留安全错误码，不再回显远端消息正文。
- 清理已被批量写入、范围查询和统一授权检查取代的旧 Python 辅助方法，并补齐常见 Python 工具缓存的忽略规则。

### 隐私与数据

- 新增默认关闭的 `allow_health_data_to_llm` 显式授权；未授权时保留所有者私聊命令，但不会向模型发送健康摘要。
- 新增默认 90 天的本地保留期、`secure_delete`、所有者双确认清除命令和清除后的 WAL/VACUUM 回收；`data_retention_days=0` 可正确关闭自动清理。
- SQLite schema 升级到 v7：同步成功/失败状态按小米 `userId` 隔离，主动消息记录和保留期清理按所有者隔离，并移除不再使用的 `care_deliveries` 表；旧健康记录保留，无法确认账号归属的旧同步状态会被清除后重新刷新。
- 删除未接入运行时且与日常关心定位冲突的体征阈值 `AlertService`。

## [v0.5.9] - 2026-07-23

### 调整

- 按 AstrBot 开发原则将小米云端适配器的日志接入 AstrBot 官方 logger；离线测试使用最小 API 桩，不影响运行时日志。

## [v0.5.8] - 2026-07-23

### 修复

- 清理文档、配置提示、测试和发布包中的个人化示例；所有公开示例均使用不可关联到用户的占位值。

## [v0.5.7] - 2026-07-23

### 调整

- 插件定位调整为日常对话增强：移除心率、血氧、压力、睡眠时长等阈值式“异常”评估和主动告警链路，不再把小米数据当作专业健康检测。
- Bot 会在睡眠、疲劳、早晚问候、加班、运动和深夜聊天等日常对话线索出现时，在后台准备最小必要的已同步生活数据，并只在有助于自然交流时融入回复。
- 后台主动消息仅保留深夜仍有私聊活动等自然关心时机，继续受事件去重、冷却和每日上限保护；措辞仍由所选模型与人格生成。
- 配置页、帮助文本和 README 统一改为“生活数据 / 日常关心”表述，并清理不再使用的体征阈值配置。
- 补充小米手机活动数据说明：请关闭手机侧“运动健康”参与的计步/活动数据来源、保留手环同步应用，避免手机本地合并结果未完整上传云端而使步数、距离和活动消耗与插件不一致。
- 按 AstrBot 官方 WebUI 更新安装说明，分别提供“从链接安装”和“从文件安装”步骤，并补充插件市场安装入口。
- 按官方开发指南完善插件市场短描述、稳定作者命名空间，并改用 AstrBot 官方日志接口。

## [v0.5.6] - 2026-07-23

### 修复

- 依据 AstrBot 插件配置手册，为四项模型/人格配置增加 `_special: select_provider` 与 `_special: select_persona`。插件配置页现在直接下拉展示已配置的聊天模型和人格，不再要求手动复制 Provider ID 或 `persona_id`。

## [v0.5.5] - 2026-07-23

### 新增

- 新增 `health_dialogue_provider_id` 与 `health_dialogue_persona_id`：可为睡眠、心率等自然语言健康对话单独选择 AstrBot 聊天模型与人格。配置后先生成受事实约束的健康解读草稿，再交给正常对话流程回复。
- 新增 `proactive_reminder_provider_id` 与 `proactive_reminder_persona_id`：可为后台主动关心独立选择模型和人格；留空则继续使用所有者私聊的当前模型和人格。
- 配置页和 README 明确说明自定义模型会接收相应的已同步健康数据，便于用户按隐私边界选择服务提供商。

## [v0.5.4] - 2026-07-23

### 改进

- 主动健康流程改为“后台同步与规则判定 → 命中条件后调用当前私聊的聊天模型与人格 → 发送一两句自然关心”。不再把原始规则说明、技术免责声明和固定模板直接发给用户；模型或人格不可用时宁可跳过，不会退化为模板消息。
- 主动判断仍只基于配置阈值、足够新的云端样本、深夜时段和近期私聊活动，并保留事件去重、冷却和每日上限；LLM 仅负责措辞，不能自行触发提醒或编造健康数据。
- 根据 AstrBot 插件约定加入根目录 `logo.png`，使用用户提供图像制作的方形插件图标。

## [v0.5.3] - 2026-07-22

### 修复

- 修复“今日健康”和自然语言中的当日/昨日心率统计：按 `user_timezone` 的本地自然日（`00:00–24:00`）读取完整心率样本，不再误用滚动 24 小时窗口或默认 100 条记录上限。
- 修复“健康趋势”的日均心率按 UTC 日期归类的问题，改为按用户本地日期计算，并继续排除运动心率样本以保持趋势口径。
- 明确输出“今日心率（本地自然日）”，便于与小米运动健康 App 当日页面对照。

## [v0.5.2] - 2026-07-22

### 修复

- 将活动、心率、体重、血氧、压力、心率记录、主动提示与“最近同步完成时间”统一按 `user_timezone` 显示，不再直接向用户展示数据库中的 UTC `+00:00` 原始值。
- 明确区分“数据采集时间”和“最近同步完成时间”，避免把小米云记录的采样时刻误认为插件执行同步的时刻。

## [v0.5.1] - 2026-07-22

### 修复

- 修复直接复制 `/sid` 输出时，UID 或 Bot ID 因携带标签、空白、括号/引号而误拒绝真实所有者私聊的问题；`owner_platform_instance_id` 仍为必填，并继续与 UID 执行精确双重校验。
- 使用 AstrBot 公共 `event.get_message_type()` 识别 `FriendMessage`，并分别报告 UID、Bot ID 与消息类型不匹配。LLM 工具不再用同一句话混淆“身份不匹配”和“群聊”。
- 配置中的 UID 与 Bot ID 会去除首尾空白、`/sid` 标签和常见成对括号/引号，避免直接复制 `[示例 UID]` 或 `「示例 Bot 名称」` 后授权失败。
- 小米云分页返回重复游标或达到安全上限时，睡眠等独立样本会保留已经取得并去重的记录；步数/热量每日聚合仍拒绝不完整结果，避免用部分页覆盖正确缓存。
- `/健康同步` 显示单项数据的脱敏失败原因；自然语言睡眠查询在缓存为空时只说明“暂无已同步记录”，不再诱导模型声称设备不支持。
- 修复初次登录返回 401/403 时未进入凭证失效暂停逻辑，以及睡眠/压力评分为合法 `0` 时被误当成缺失的问题。

### 验证

- 增加私聊授权回归矩阵：缺失/正确/错误 Bot ID、正确/错误 UID、`FriendMessage` 与 `GroupMessage`，并确认运行时事件 ID 不会被配置清洗逻辑折叠。
- 增加 `sleep` 云端解析 → 同步服务 → SQLite → 自然语言快照的完整离线回归测试。

## [v0.5.0] - 2026-07-22

### 新增

- 增加主动健康检查，默认每 30 分钟刷新一次短时间窗口，仅在存在有依据的情况且不在冷却期时私聊所有者。
- 增加个人心率、血氧、压力与睡眠时长阈值。`0` 表示关闭对应指标，不使用适用于所有人的硬编码医学阈值。
- 心率只评估新鲜、连续、非运动的被动样本；血氧和压力要求连续新鲜样本；睡眠只评估最近 36 小时内完成的一段记录。
- 增加深夜私聊活动检查。它只依据所有者近期确实发送过私聊消息，不会把“云端没有睡眠记录”推断成“没有睡觉”，每晚最多触发一次。
- 增加同事件去重、同类型冷却、全局主动消息冷却与每日上限。多项情况会合并为一条固定安全文本，并且仅在平台确认发送成功后记录提醒状态。
- 增加必填的 `owner_platform_instance_id` 校验，避免不同平台出现相同用户 ID 时越权读取健康数据。

### 对话、数据与可靠性

- 可在所有者私聊的普通对话中调用 `query_mi_fitness_health`，直接询问昨天睡眠、今日步数或最近心率；缓存过期时按需刷新小米云，不再要求先输入同步命令。
- 稳定读取步数、距离、活动消耗、心率、体重与部分身体成分，并增加睡眠、血氧和压力的兼容性读取；后三类取决于账号实际暴露的云端 schema。连接状态会显示最近 30 天实际探测到的类型。
- 保留 `resting_heart_rate` 心率回退，兼容部分账号没有标准心率采样 key 的情况。
- 对话快照只向模型提供本次问题相关的类别，减少无关健康信息暴露。
- SQLite 升级至 schema v4，无损增加提醒事件去重字段与索引，并启用 WAL 和 30 秒 busy timeout。
- 临时云端错误采用有上限的退避重试；凭证失效、需要验证或触发风控时暂停后台检查，等待重新授权。

### 隐私与安全

- 所有健康数据命令、LLM 工具和上下文注入只允许配置的所有者在私聊中使用；群聊不会返回健康数据。
- 主动消息只发送到插件最近记录的所有者私聊会话，使用固定模板，不交给 LLM 二次生成。
- `pass_token` 配置声明为密码字段。它等同登录凭证，不得发送到聊天、日志、截图或 Issue；请只在受信任的 AstrBot 管理页面填写。输入遮罩不等于加密存储；若所用 AstrBot WebUI 未正确遮罩或需要更严格保护，可留空配置并改用 `MI_FITNESS_PASS_TOKEN` 环境变量。

### 从 v0.4.x 升级

- 数据库会自动迁移到 schema v4，不会主动删除已有健康记录。
- 新增 12 项配置：`owner_platform_instance_id`、`enable_proactive_health_monitor`、`health_check_interval_minutes`、`proactive_daily_limit`、`enable_late_night_activity_check`、`late_night_start`、`late_night_end`、`late_night_activity_window_minutes`、`spo2_low`、`stress_high`、`sleep_min_minutes`、`alert_data_max_age_minutes`。
- `enable_health_alerts` 的默认值由 `false` 改为 `true`，但心率、血氧、压力和睡眠阈值默认均为 `0`，因此升级后这些阈值提示仍然关闭。
- `enable_proactive_health_monitor` 与 `enable_late_night_activity_check` 默认开启。所有者需先私聊机器人一次，插件才能记录主动发送目标。
- 如果要停止周期性小米云请求，需同时关闭 `enable_proactive_health_monitor` 和 `enable_auto_sync`；自然语言健康查询仍可能按需刷新。
- v0.5.0 起必须配置 `owner_platform_instance_id`；所有者私聊发送 `/sid` 后，将 `UID` 填入 `owner_platform_id`、将 `Bot ID` 填入该项。任一项为空时，所有健康数据命令、对话工具和主动监控都不会启用。升级后群聊健康查询也会被拒绝，这是有意收紧的隐私边界。

### 已知限制

- 手机端仍需先把手环或手机数据同步到小米运动健康云。插件会额外回看 48 小时以处理延迟上传和历史修正；睡眠数据通常在醒来后才上传。
- AstrBot 的 QQ 官方 API 适配器不支持当前主动发送方式；AIOCQHTTP/NapCat 等通常支持。普通私聊中的健康查询不受此限制。
- 不同小米账号、区域与设备可能暴露不同的云端 key；缺失或未知字段不会被伪造。

[v0.5.7]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.7
[v0.5.6]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.6
[v0.5.5]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.5
[v0.5.4]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.4
[v0.5.3]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.3
[v0.5.2]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.2
[v0.5.1]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.1
[v0.5.0]: https://github.com/utrgdfg/astrbot_plugin_mi_fitness_health/releases/tag/v0.5.0
