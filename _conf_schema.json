{
  "user_id": {
    "type": "string",
    "description": "小米账号 userId（必填）",
    "hint": "登录 account.xiaomi.com 后，在浏览器开发者工具的 Cookies 中复制 userId 的值。",
    "obvious_hint": true,
    "default": ""
  },
  "pass_token": {
    "type": "string",
    "password": true,
    "description": "小米账号 passToken（必填、敏感）",
    "hint": "在同一 Cookies 列表复制 passToken 的值。它相当于登录凭证，只能填写在你自己的插件配置页，不要发送到聊天、截图或 Issue。",
    "obvious_hint": true,
    "default": ""
  },
  "owner_platform_id": {
    "type": "string",
    "description": "使用者 UID（必填）",
    "hint": "私聊 Bot 发送 /sid，复制回复中的 UID。只有这个使用者可以读取健康数据。",
    "obvious_hint": true,
    "default": ""
  },
  "owner_platform_instance_id": {
    "type": "string",
    "description": "机器人 Bot ID（必填）",
    "hint": "从同一条 /sid 回复中复制 Bot ID，用于确认健康数据只由当前机器人处理。",
    "obvious_hint": true,
    "default": ""
  },
  "region": {
    "type": "string",
    "description": "小米健康云区域（可选）",
    "hint": "不确定时先留空让插件自动识别。自动识别失败时再选择 cn、ru、de、i2、sg 或 us。",
    "options": ["", "cn", "ru", "de", "i2", "sg", "us"],
    "default": ""
  },
  "user_timezone": {
    "type": "string",
    "description": "用户所在时区",
    "hint": "用于判断“今天”和“昨天”。中国大陆通常保持 Asia/Shanghai。",
    "default": "Asia/Shanghai"
  },
  "allow_health_data_to_llm": {
    "type": "bool",
    "description": "允许聊天模型接收生活数据摘要",
    "hint": "日常对话增强必须开启此项。插件只会向当前私聊模型提供与话题有关的少量数据，不会要求模型固定输出数据报告。第三方模型服务可能处理或保存内容，请只在信任所用模型时开启。",
    "obvious_hint": true,
    "default": false
  },
  "enable_care_dialogue": {
    "type": "bool",
    "description": "在日常对话中使用生活数据",
    "hint": "开启后，普通聊天出现作息、疲劳、运动、早晚问候等线索时，插件会在后台准备相关数据，让 Bot 按当前人格自然回应。用户不需要主动询问数据。",
    "default": true
  },
  "context_decision_provider_id": {
    "type": "string",
    "description": "生活数据调用判断模型（可选）",
    "hint": "直接选择 AstrBot 中已有的聊天模型。它会在所有者私聊时判断当前消息是否需要生活数据以及需要哪些类别，只接收当前消息，不接收小米数据；建议选择响应快、成本低的模型。留空时使用插件内置规则。",
    "_special": "select_provider",
    "default": ""
  },
  "context_decision_prompt": {
    "type": "text",
    "description": "生活数据调用判断提示词",
    "hint": "告诉判断模型什么情况下需要调取生活数据。插件仍会强制限制输出格式、数据类别和隐私边界；留空时恢复默认提示词。",
    "default": "判断当前所有者私聊是否确实需要小米运动健康生活数据来增强回复。适合调用：用户自身的作息、睡眠、疲劳、精力、散步、锻炼、运动恢复、心率、体重、身体成分、血氧或压力等话题。不适合调用：无关闲聊、知识问答、代码任务、第三方情况、医疗紧急情况，或生活数据对当前回复没有明确帮助时。早晚问候不要机械调用；普通疲劳不要自动等同于心率问题。没有必要时优先不调用。"
  },
  "health_dialogue_provider_id": {
    "type": "string",
    "description": "补充回复草稿使用的模型（可选）",
    "hint": "直接选择 AstrBot 中已有的聊天模型。仅在对话需要单独生成生活化回复草稿时使用；普通聊天仍由当前会话模型回复。留空时沿用当前私聊模型。",
    "_special": "select_provider",
    "default": ""
  },
  "health_dialogue_persona_id": {
    "type": "string",
    "description": "补充回复草稿使用的人格（可选）",
    "hint": "直接选择 AstrBot 中已有的人格。仅用于可选的生活化回复草稿；普通聊天仍保持当前私聊人格。留空时沿用当前私聊人格。",
    "_special": "select_persona",
    "default": ""
  },
  "natural_query_sync_minutes": {
    "type": "int",
    "description": "对话触发的数据刷新间隔（分钟）",
    "hint": "日常聊天需要相关数据且缓存较旧时，插件会在后台刷新。默认 15 分钟；设置太短会增加小米云请求次数。",
    "default": 15
  },
  "enable_proactive_health_monitor": {
    "type": "bool",
    "description": "开启主动关心检查",
    "hint": "开启后定期检查已有的本地数据与近期私聊活动，只在合适的日常时机私聊关心；此项不会定期读取小米云。还需要开启“允许聊天模型接收生活数据摘要”。",
    "default": true
  },
  "proactive_reminder_provider_id": {
    "type": "string",
    "description": "主动关心使用的模型（可选）",
    "hint": "直接选择 AstrBot 中已有的聊天模型。它会先结合最近少量私聊上下文判断是否应该发送，再为通过判断的主动关心生成措辞。留空时沿用当前私聊模型。",
    "_special": "select_provider",
    "default": ""
  },
  "proactive_decision_prompt": {
    "type": "text",
    "description": "深夜主动关心判断提示词",
    "hint": "告诉主动关心模型什么情况下应该发送或保持安静。模型会收到候选时机和最近少量私聊文本；插件会强制使用布尔 JSON，并在模型失败、上下文为空或拿不准时不发送。留空时恢复默认提示词。",
    "default": "判断此刻是否值得主动给用户发送一条深夜关心。这是发送前的最后一道闸门，应优先避免打扰。以下情况必须不发送：用户最近表示要睡觉、晚安、休息、离开或不想被打扰；机器人已经对同一件事表达过关心；对话已经自然结束；主动消息会重复上一句、违背用户意图或重新开启已经结束的话题；仅仅因为深夜有过消息活动但没有明确关心价值。只有当用户仍在积极交谈，并且最近上下文显示一条简短、自然、不重复的关心此刻确实有帮助时才发送。拿不准时不要发送。"
  },
  "proactive_reminder_persona_id": {
    "type": "string",
    "description": "主动关心使用的人格（可选）",
    "hint": "直接选择 AstrBot 中已有的人格。留空时沿用当前私聊人格。",
    "_special": "select_persona",
    "default": ""
  },
  "health_check_interval_minutes": {
    "type": "int",
    "description": "主动关心检查间隔（分钟）",
    "hint": "默认每 30 分钟检查一次本地数据与近期私聊活动，不会因此读取小米云。",
    "default": 30
  },
  "enable_late_night_activity_check": {
    "type": "bool",
    "description": "允许在深夜活跃时关心",
    "hint": "只在使用者最近确实发过私聊消息时触发，不会因为没有睡眠记录就猜测你没睡。",
    "default": true
  },
  "late_night_start": {
    "type": "string",
    "description": "深夜关心开始时间",
    "hint": "使用 24 小时制，例如 00:30。",
    "default": "00:30"
  },
  "late_night_end": {
    "type": "string",
    "description": "深夜关心结束时间",
    "hint": "使用 24 小时制，例如 06:00。",
    "default": "06:00"
  },
  "late_night_activity_window_minutes": {
    "type": "int",
    "description": "多久内发过消息算仍在活跃（分钟）",
    "hint": "默认 45 分钟。超过这段时间没有私聊活动就不会触发。",
    "default": 45
  },
  "care_cooldown_minutes": {
    "type": "int",
    "description": "两次主动关心至少间隔多久（分钟）",
    "hint": "默认 120 分钟，避免频繁打扰。",
    "default": 120
  },
  "proactive_daily_limit": {
    "type": "int",
    "description": "每天最多主动关心几次",
    "hint": "达到次数后当天不再主动发消息，默认 3 次。",
    "default": 3
  },
  "enable_auto_sync": {
    "type": "bool",
    "description": "开启普通自动同步",
    "hint": "开启后，插件会定期从小米云更新本地数据。",
    "default": false
  },
  "sync_interval_minutes": {
    "type": "int",
    "description": "普通自动同步间隔（分钟）",
    "hint": "只开启普通自动同步时使用，默认每 60 分钟读取一次小米云。",
    "default": 60
  },
  "default_sync_days": {
    "type": "int",
    "description": "每次同步最近几天",
    "hint": "通常保持 7 天即可，插件会额外处理延迟上传的数据。",
    "default": 7
  },
  "data_retention_days": {
    "type": "int",
    "description": "本地数据保留天数",
    "hint": "默认 90 天；设为 0 可关闭自动清理。",
    "default": 90
  },
  "database_path": {
    "type": "string",
    "description": "本地数据保存位置（高级、可选）",
    "hint": "普通用户请留空，插件会自动使用 AstrBot 的插件数据目录。",
    "default": ""
  }
}
